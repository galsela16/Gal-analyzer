# GAL Analyzer 5.5.74

- Fixed the false “another measurement is active” alert during Loopback Auto Sync.
- Non-blocking busy handling is now applied to the actual delay recorder.
- Restored the Area measurement busy guard after locating the misplaced change.
- Regression coverage now validates the exact functions instead of matching an unrelated code occurrence.
