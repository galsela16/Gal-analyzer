# GAL Analyzer — תיקון זיהוי Delay ב-Sweep (v5.5.0)

## הבעיה
מדידת Delay נכשלה בשטח עם ההודעה **"לא נמצא זמן הגעה ברור"** כשהאות היה **Sweep**.

הסיבה: הגנרטור מנגן Sweep כאוסצילטור סינוס יחיד, כלומר בכל רגע יש תדר בודד.
מנוע ה-Delay הישן (`computeDelay`) חותך את הקאפצ'ר ל-chunks קצרים (~170ms) ומריץ
GCC-PHAT על כל chunk בנפרד, ודורש עשרות bins פעילים רחבי-פס בכל chunk. chunk בודד
של Sweep הוא צר-פס, ולכן אף פעם לא עבר את סף רוחב-הפס — והמדידה נדחתה, גם כשהכול תקין.

## התיקון
נוסף מנוע `computeSweepDelay` שמבצע **GCC-PHAT על כל הקאפצ'ר כולו** — שם ה-Sweep
עובר את כל התחום ומייצר פסגת קורלציה חדה במיוחד. הוא כולל:

- חלון Tukey קל (10% בקצוות) למניעת דליפה ספקטרלית בקצוות הקאפצ'ר.
- אינטרפולציה פרבולית לדיוק תת-דגימה.
- שלושה חלונות רחבים חופפים לבדיקת יציבות אמיתית שנשארת רחבת-פס גם ב-Sweep.
- ציוני separation ו-activeBins לדחיית מדידות לא אמינות.

`computeStableDelay` עודכן כך ש:
1. **Sweep** → מנותב ישירות למנוע החדש.
2. **רעש ורוד/לבן** → נשאר במסלול ה-chunk המוכח (ללא שינוי).
3. **אות לא ידוע ('auto', למשל מוזיקה)** → מנסה קודם chunk, ואם לא נעל — נופל
   אוטומטית לקורלציה על כל הקאפצ'ר כ-rescue.

`runDelayCapture` שולח כעת רמז `signalType` לפי מצב הגנרטור בזמן המדידה.

## אימות
`scripts/validate-measurement-engines.mjs` הורחב עם 9 מקרי Sweep (44.1/48/96kHz,
עיכובים 2/5/12ms, כולל החזרות ורעש) + מקרה דחייה של Sweep לא-קוהרנטי. כל הבדיקות
עוברות (27 מקרי delay סה"כ), וכל שאר המנועים (קליברציה, Sub/Top, TF, RT60, EQ)
ממשיכים לעבור ללא שינוי.

## מה השתנה בקבצים
- `js/app-core.js` — מנוע `computeSweepDelay` חדש + עדכון `computeStableDelay` ו-`runDelayCapture`.
- `scripts/validate-measurement-engines.mjs` — בדיקות Sweep.
- `sw.js` + `js/core/config.js` — עדכון גרסה ל-5.5.0 (מרוקן cache ישן אצל משתמשים).

## הערה
תיקיית `.github` (אם קיימת) לא נכללה בחבילה הזו — שמור את זו שכבר יש לך בריפו.
